Corrige el grave error de maquetación y superposición (overlap) en la pantalla de "Nueva Venta" (`apps/cajero/components/pos/pos-terminal.tsx`).

**PROBLEMA VISUAL DETECTADO:**
1. El panel derecho "TICKET EN CURSO" se está posicionando por encima (encaramado) del menú del catálogo en la izquierda, tapando los botones de categorías, sabores, tamaños y toppings.
2. Los botones de sabores y opciones se están estrechando de más y truncando el texto (ej. "Capu", "Limo brasi", "Extragran").
3. El botón inferior "ENVIAR Y COBRAR" se desborda en el pie sobreponiéndose a las etiquetas de TOTAL y a los elementos del fondo.

**SOLUCIÓN A IMPLEMENTAR EN `apps/cajero/components/pos/pos-terminal.tsx`:**

1. **Estructura Contenedora Principal:**
   - Cambia el contenedor raíz a un flexbox o grid responsive con scroll independiente:
     `className="flex flex-col lg:flex-row w-full h-[calc(100vh-4rem)] overflow-hidden bg-slate-950"`
   - Elimina cualquier propiedad de posicionamiento absoluto (`absolute`, `fixed`, `right-0`) en los contenedores hijos que esté provocando la superposición.

2. **Panel Izquierdo - Catálogo de Menú (`flex-1 min-w-0`):**
   - Dale al panel izquierdo del catálogo: `className="flex-1 overflow-y-auto p-4 flex flex-col gap-6 min-w-0"`
   - **Pestañas de Categorías:** Usa un grid/flex adaptable: `className="flex flex-wrap gap-2"` con botones de `h-12 px-4 text-base font-bold`.
   - **Grilla de Sabores:** Configura un grid adaptativo real para que el texto no se trunque: `className="grid grid-cols-2 sm:grid-cols-3 xl:grid-cols-4 gap-3"`. Los botones de sabor deben ser `className="h-16 w-full text-sm font-bold leading-tight p-2 whitespace-normal break-words"`.
   - **Secciones (Tamaño, Boba, Extras):** Mantén grids ordenados (`grid grid-cols-2 gap-2`) para que los botones tengan espacio suficiente sin amontonarse.

3. **Panel Derecho - Ticket en Curso (Panel Fijo y Separado):**
   - Dale al contenedor del ticket una estructura rígida que NUNCA monte al catálogo:
     `className="w-full lg:w-[380px] xl:w-[420px] flex-shrink-0 bg-slate-900 border-t lg:border-t-0 lg:border-l border-slate-800 flex flex-col justify-between p-4 h-full overflow-hidden z-10"`
   - **Cuerpo del Ticket:** Encierra la lista de ítems agregados en un div scrolleable: `className="flex-1 overflow-y-auto my-2 space-y-2"`.

4. **Pie del Ticket (Controles y Botón de Cobro):**
   - Agrupa el input de cliente, botones Mesa/Llevar, Total y el botón final en un bloque inferior fijo (`flex flex-col gap-3 pt-3 border-t border-slate-800`):
   - El botón "ENVIAR Y COBRAR" debe tener `className="w-full h-16 text-xl font-black bg-blue-600 hover:bg-blue-700 text-white rounded-xl shadow-lg mt-2 flex items-center justify-center gap-2"`. Asegúrate de que no tenga márgenes negativos ni posicionamiento flotante que tape el texto del Total.