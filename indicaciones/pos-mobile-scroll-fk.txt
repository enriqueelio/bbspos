Corrige la experiencia táctil en móviles para la pantalla de "Nueva Venta", mejora los botones de control de cantidad y soluciona el error de llave foránea al crear el pedido.

**1. Botones Masivos en el Ticket (+ / - y Quitar)**
Abre `apps/cajero/components/pos/pos-terminal.tsx` (o el componente donde se renderizan los ítems del carrito, como `CartItem` si está modularizado).
- Localiza los botones de incrementar (`+`), decrementar (`-`) y `Quitar`.
- Modifica sus clases para que sean fáciles de tocar en una tablet de 8 pulgadas:
  - Para los botones `+` y `-`: Usa `className="h-14 w-14 rounded-lg bg-slate-700 text-white text-2xl font-bold flex items-center justify-center active:scale-95 transition-transform"`.
  - Para el valor numérico (cantidad): Usa `className="text-xl font-bold w-8 text-center"`.
  - Para el botón `Quitar`: Usa `className="h-14 px-6 rounded-lg bg-red-600 text-white text-lg font-bold active:scale-95 transition-transform"`.

**2. Refactorización a Flujo de Scroll Único (UX Móvil)**
Abre `apps/cajero/components/pos/pos-terminal.tsx`.
- Elimina las alturas fijas restrictivas y los bloqueos de overflow en el contenedor principal. Cambia el contenedor padre a: `className="flex flex-col lg:flex-row w-full min-h-screen bg-slate-950 gap-4 pb-32 lg:pb-0"`.
- Al Panel Izquierdo (Catálogo), quítale el `overflow-y-auto`. Déjalo fluir naturalmente con la página: `className="flex-1 p-4 flex flex-col gap-6"`.
- Al Panel Derecho (Ticket en Curso), quítale el `h-[50vh]` y el scroll interno forzado. Haz que se apile debajo del catálogo: `className="w-full lg:w-[420px] flex-shrink-0 bg-slate-900 lg:border-l border-slate-800 flex flex-col p-4 gap-4"`.

**3. Fijar el Pie de Cobro en Móviles**
En el mismo `pos-terminal.tsx`, envuelve la sección del Total y el botón "Enviar a Caja/Cobrar" en un contenedor fijado al fondo para que el mesero siempre lo tenga a mano:
`className="fixed bottom-0 left-0 right-0 p-4 bg-slate-900 border-t border-slate-800 z-50 lg:static"`

**4. Solución al Error Prisma (Foreign Key Constraint)**
Abre `apps/cajero/actions/pos.ts` en la función `createPosOrder`.
- Antes de crear la orden, verifica que el usuario exista: 
  `const dbUser = await prisma.user.findUnique({ where: { id: session.user.id } });`
- Al hacer el `prisma.order.create()`, usa `userId: dbUser ? dbUser.id : null`.
- Envuelve esto en un bloque `try/catch`. En el `catch`, devuelve un error amigable al cliente en lugar de un fallo genérico del servidor.