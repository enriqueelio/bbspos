Modifica `apps/cajero/components/split-payment-dialog.tsx`. Elimina el input nativo `<input type="number">`. Reemplázalo por un `<div className="text-4xl font-mono text-emerald-400 p-4 bg-slate-900 rounded-lg text-right">`. Debajo, crea un teclado numérico: `<div className="grid grid-cols-3 gap-2 mt-4">` con botones del 0 al 9 y un botón de "Borrar". Cada botón debe ser `<Button variant="secondary" className="h-16 text-2xl font-bold" type="button" onClick={() => handleNumpad(num)}>`.