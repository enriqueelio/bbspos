{
  "name": "bubba",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @bubba/db generate",
    "db:migrate": "pnpm --filter @bubba/db migrate",
    "db:seed": "pnpm --filter @bubba/db seed",
    "daily-report": "pnpm --filter @bubba/db daily-report",
    "telegram-alert": "pnpm --filter @bubba/db telegram-alert",
    "db:sync": "scripts\\db-sync.bat",
    "setup-env": "node scripts/setup-env.mjs",
    "proxy": "node scripts/https-proxy/proxy.mjs"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "http-proxy": "^1.18.1",
    "sharp": "^0.35.4",
    "turbo": "^2.5.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@iconify/react": "^6.0.2"
  }
}
